<?php
/**
 * @file
 * Install, update, and uninstall functions for the ${File_name} module.
 */

/**
 * Implements hook_requirements().
 */
function ${File_name}_requirements(${DS}phase) {
  ${DS}requirements = array();
  ${DS}t = get_t();

  // Noop.
  return ${DS}requirements;
}

/**
 * Implements hook_schema().
 */
function ${File_name}_schema() {
  ${DS}schema = array();
  ${DS}t = get_t();

  // Noop.
  return ${DS}schema;
}

/**
 * Implements hook_install().
 */
function ${File_name}_install() {
  ${DS}t = get_t();

  // Noop.
}

/**
 * Implements hook_uninstall().
 */
function ${File_name}_uninstall() {
  ${DS}t = get_t();

  // Noop.
}

/**
 * Installs ${File_name} module.
 */
function ${File_name}_update_7000(&${DS}sandbox) {
  ${DS}t = get_t();

  // Noop to ensure Drupal stores a schema version in the database.
}
