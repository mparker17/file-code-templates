name: ${Module_label}
description: ${Module_description}.
package: Other

type: module
core: 8.x

dependencies:
  - "drupal:system (>=8.6.0)"

configure: ${Module_machine_name}.settings

hidden: false
