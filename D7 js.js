/**
 * @file
 * Client-side script for the ${Module_machine_name} module.
 */
(function ($) {

  "use strict";

  Drupal.behaviors.${Module_machine_name} = {
    attach: function (context, settings) {
    },

    detach: function (context, settings, trigger) {
    }
  };

})(jQuery);
